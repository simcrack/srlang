package ch.richebach.srlang;

/**
 * Operatoren für binäre Ausdrücke (sowohl arithmetische als auch boolsche).
 */
public enum BinOperator {
    ADD,
    SUB,
    MUL,
    DIV,
    EQU,
    LES,
    GTR
}
